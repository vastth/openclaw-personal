# 交接记录 - 2026-03-08

### 时间戳

- 时间戳: 2026-03-08
- From Agent: Copilot
- To Agent: Claude Code / 用户
- 任务 ID: OC-021
- 任务标题: Claw 自管理与自升级能力落地
- 状态: REVIEW

### 已完成内容

- 摘要: 为主工作区 agent 落地“收到消息后可自行装插件 / 自行升级 OpenClaw”的最小闭环。具体包括：补 exec 白名单、在 `workspace/SOUL.md` 授权 agent 可自管理、在 `workspace/AGENTS.md` 写入可执行工作流，并补齐今日看板与 handoff。
- 改动文件:
  - `exec-approvals.json`
  - `workspace/SOUL.md`
  - `workspace/AGENTS.md`
  - `docs/TASK_BOARD.md`
  - `docs/handoffs/2026-03-08.md`
  - `docs/handoffs/archive/2026-03-08.md`
- 行为变化:
  - 为所有 agent 放行 `openclaw`、`openclaw.cmd`、`npm`、`npm.cmd` 四条 exec allowlist，覆盖插件管理与升级链路
  - 在 `workspace/SOUL.md` 中新增 Self-Management 章节，明确 agent 可直接执行插件安装、版本升级与结果回报
  - 在 `workspace/AGENTS.md` 中新增“自我管理”工作流，覆盖安装、启用、禁用、卸载、升级与安全边界

### 验证情况

- 执行命令:
  - `openclaw approvals allowlist add "C:/Users/Administrator/AppData/Roaming/npm/openclaw"`
  - `openclaw approvals allowlist add "C:/Users/Administrator/AppData/Roaming/npm/openclaw.cmd"`
  - `openclaw approvals allowlist add "C:/Program Files/nodejs/npm"`
  - `openclaw approvals allowlist add "C:/Program Files/nodejs/npm.cmd"`
  - `openclaw approvals get`
  - `openclaw plugins list`
  - `openclaw gateway status`
- 结果:
  - PASS: `openclaw approvals get` 显示 allowlist=4，四条路径均已落盘到 `exec-approvals.json`
  - PASS: `openclaw plugins list` 正常返回插件清单，说明插件管理链路可用
  - PASS: `openclaw gateway status` 返回 `RPC probe: ok`，当前 gateway 正在本机 `127.0.0.1:18789` 监听
- 尚未执行:
  - 真实消息面验证“在飞书中发一句升级 claw，由 agent 实际触发 `npm update -g openclaw`”
  - Claude Code `/oc-review` 边界复核

### 关键决策

- 决策 1: 不只放行 `openclaw`，同时放行 `npm` 与 `npm.cmd`
- 原因: 仅放行 `openclaw` 只能覆盖插件管理，不能覆盖 `npm update -g openclaw` 这条自升级链路；否则“可自升级”会停留在文档层。
- 放弃方案: 只保留 `openclaw` allowlist，升级时再手动审批。

- 决策 2: 将“安装/升级”的权限写进 `workspace/SOUL.md` 和 `workspace/AGENTS.md`
- 原因: 仅有 allowlist 不足以形成稳定行为；需要把授权边界和操作顺序写进主提示，避免 agent 每次都退回到“先问一遍”的保守模式。
- 放弃方案: 只改运行态 allowlist，不改工作区提示。

### 风险与回滚

- 风险: 自管理能力扩大了 agent 的本机执行面，虽然目前只放开 `openclaw` 与 `npm` 两条链路，但若未来在提示词中加入更多“自动安装/升级”动作，仍需继续约束卸载和外部写操作边界。
- 回滚点（commit / 文件备份）: 手动删除 `exec-approvals.json` 中对应 allowlist 项，或执行 `openclaw approvals allowlist remove <pattern>`；工作区提示词可直接回退 `workspace/SOUL.md` 与 `workspace/AGENTS.md` 本次新增章节。

### 下一步动作（第一条命令）

- `/oc-review OC-021`

### 给下一位 Agent 的说明

- 待确认问题: 当前“允许自升级”是否还需要进一步收紧，例如只允许 `npm update -g openclaw` 而不是整个 `npm(.cmd)` 路径。
- 前提假设: 当前主要目标是让主工作区 agent 在你的私聊里能完成“装插件 / 升级 claw”两类动作；不是放开任意系统级安装权限。
- 必读上下文文件:
  - `docs/TASK_BOARD.md`
  - `docs/handoffs/2026-03-08.md`
  - `workspace/SOUL.md`
  - `workspace/AGENTS.md`

---

### 时间戳

- 时间戳: 2026-03-08
- From Agent: Copilot
- To Agent: Claude Code / 用户
- 任务 ID: OC-022
- 任务标题: 主脑路由收敛与重任务 Agent 预留
- 状态: REVIEW

### 已完成内容

- 摘要: 按当前工作站的长期方案对 `openclaw.json` 做了三项收敛：保留 `qwen2.5:7b` 作为 `main` 默认主脑；将 `agents.defaults.compaction.mode` 从 `safeguard` 切到 `default`，减少每轮对话出现的无效 safeguard 提示；新增一个不绑定渠道的 `heavy` 云端 agent，同时在 `agents.defaults.memorySearch` 预留正式的向量检索 / embedding 配置位。
- 改动文件:
  - `openclaw.json`
  - `docs/CONFIG_GUIDE.md`
  - `docs/TASK_BOARD.md`
  - `docs/handoffs/2026-03-08.md`
  - `docs/handoffs/archive/2026-03-08.md`
- 行为变化:
  - `main` 默认模型继续保持 `ollama/qwen2.5:7b`
  - `compaction.mode` 改为 `default`，不再走 safeguard 专用摘要保护模式
  - 新增 `heavy` agent，默认模型为 `deepseek/deepseek-chat`，供高复杂度任务手动切换或未来路由
  - 新增 `agents.defaults.memorySearch` 配置段，当前 `enabled=false`，仅作为未来向量检索 / embedding 接入点

### 验证情况

- 执行命令:
  - `openclaw config validate`
  - `openclaw models status`
  - `openclaw models status --agent heavy`
  - `openclaw gateway restart`
  - `openclaw gateway status`
  - `openclaw agent --agent main --local --message "只回复 OK" --thinking off`
- 结果:
  - PASS: `openclaw config validate` 通过，说明 `memorySearch` 与 `heavy` agent 的字段组合符合当前 schema
  - PASS: `openclaw models status` 显示 main 默认模型仍为 `ollama/qwen2.5:7b`
  - PASS: `openclaw models status --agent heavy` 显示新 agent 默认模型为 `deepseek/deepseek-chat`
  - PASS WITH NOTE: `openclaw gateway restart` 因旧进程占用 18789 端口而超时，但随后 `openclaw gateway status` 返回 `RPC probe: ok`，说明现有 gateway 仍健康运行
  - PASS WITH NOTE: `openclaw agent --agent main --local --message "只回复 OK"` 返回有效回复，证明主链路未被本次配置破坏；但由于工作区仍存在首次引导内容，回复里仍带出 bootstrap 提示，后续可单独清理 workspace 启动文件
- 尚未执行:
  - 在飞书真实对话里验证 `compaction-safeguard` 提示是否已消失
  - 启用 `memorySearch` 后的首次 `memory index` / embedding 连通性验证

### 关键决策

- 决策 1: 将 `compaction.mode` 从 `safeguard` 改为 `default`
- 原因: 当前最直接的问题不是上下文真的失控，而是 safeguard 在“无真实对话可摘要”时持续刷提示，影响日常使用；`default` 是当前 schema 明确支持的正式模式。
- 放弃方案: 保持 `safeguard`，仅靠调整提示词或等长会话自然缓解。

- 决策 2: 用 `agents.defaults.memorySearch` 作为向量检索 / embedding 的正式预留位
- 原因: 这是 OpenClaw 当前版本真实存在的 schema 边界，后续无论走本地 Ollama embedding 还是远端 provider，都可以在这块继续演进，不必再迁移字段。
- 放弃方案: 自造一个 top-level `vectorSearch` 或 `embedding` 配置段。

- 决策 3: 新增 `heavy` agent 但暂不绑定任何渠道
- 原因: 先把“高复杂度任务的云端执行入口”稳定放进配置，再由你决定后续是手动切换、cron 调用，还是路由指定联系人/群。
- 放弃方案: 直接把现有 `main` 切回 DeepSeek，牺牲本地主链路的低成本与低延迟优势。

### 风险与回滚

- 风险: `compaction.mode=default` 虽然能消掉当前 safeguard 噪音，但在极长会话下会更主动做摘要压缩，真实总结质量仍需观察；`heavy` 目前未绑定渠道，所以只有显式调用时才会用到。
- 回滚点（commit / 文件备份）: `openclaw.json.bak`、`openclaw.json.pre-local-2026-03-07.bak`；或手动将 `compaction.mode` 改回 `safeguard`、删除 `heavy` agent 与 `memorySearch` 段。

### 下一步动作（第一条命令）

- `/oc-review OC-022`

### 给下一位 Agent 的说明

- 待确认问题: 是否要把 `heavy` 绑定到你与小克的某个特定私聊命令流，还是保持为“仅手动/显式调用”的云端高阶入口。
- 前提假设: 当前目标是建立长期架构骨架，而不是立即启用向量索引；所以 `memorySearch.enabled` 刻意保持关闭。
- 必读上下文文件:
  - `docs/TASK_BOARD.md`
  - `docs/handoffs/2026-03-08.md`
  - `openclaw.json`
  - `docs/CONFIG_GUIDE.md`

---

### 时间戳

- 时间戳: 2026-03-08
- From Agent: Copilot
- To Agent: Claude Code / 用户
- 任务 ID: OC-023
- 任务标题: 修正插件查询误用消息工具
- 状态: REVIEW

### 已完成内容

- 摘要: 复盘并定位了你与 claw 的最后一段对话问题，确认并非 `openclaw plugins list` 无法执行，而是 agent 把本地 CLI 查询误当成了聊天消息发送，调用了 `message` 工具并漏传必填的 `action` 字段。随后在工作区提示词中补了“本地命令必须本地执行、失败时必须说真实错误”的约束。
- 改动文件:
  - `workspace/SOUL.md`
  - `workspace/AGENTS.md`
  - `docs/TASK_BOARD.md`
  - `docs/handoffs/2026-03-08.md`
  - `docs/handoffs/archive/2026-03-08.md`
- 行为变化:
  - 明确禁止把 `openclaw plugins list`、`openclaw plugins info`、`openclaw plugins install`、`openclaw approvals ...`、`npm update -g openclaw` 这类本地命令包装成聊天消息发送
  - 明确要求失败时直接回报真实错误，不允许连续多轮只说“我再试一次”

### 验证情况

- 执行命令:
  - `openclaw plugins list`
  - `openclaw plugins info skill-creator`
  - `npm view @openclaw/skill-creator version`
  - `npm view skill-creator version`
  - 搜索 `agents/main/sessions/**/*.jsonl` 中的相关会话与工具错误
- 结果:
  - PASS: `openclaw plugins info skill-creator` 返回 `Plugin not found: skill-creator`
  - PASS: `openclaw plugins list` 的 stock 插件清单中不存在 `skill-creator`
  - PASS: `npm view @openclaw/skill-creator version` 返回 404，说明不存在官方同名 npm 包
  - PASS WITH NOTE: `npm view skill-creator version` 没有给出可直接复用的 OpenClaw 插件结论，不能据此判定为可装的 OpenClaw 插件
  - PASS: 会话文件 [agents/main/sessions/f3d2defb-a91f-4cb6-8e13-f3dca56ca7ab.jsonl](agents/main/sessions/f3d2defb-a91f-4cb6-8e13-f3dca56ca7ab.jsonl#L25) 显示根因是 `message` 工具校验失败：缺少必填 `action`，而不是飞书整体不可达
- 尚未执行:
  - 真实飞书回话验证新提示是否彻底阻止类似误用
  - 底层代码级修复（当前仅为工作区行为约束）

### 关键决策

- 决策 1: 不尝试“盲装” `skill-creator`
- 原因: 当前 stock 插件列表、`plugins info`、官方 npm 命名空间都证明它不存在；继续安装只会引入不确定第三方包风险。
- 放弃方案: 直接执行 `openclaw plugins install skill-creator` 碰运气。

- 决策 2: 用工作区提示约束修正行为，而不是先碰通道配置
- 原因: 根因在 agent 行为层，证据是它调用了错误的 `message` 工具参数；不是飞书全链路挂了。
- 放弃方案: 先去重配飞书 app 或重装 feishu 插件。

### 风险与回滚

- 风险: 这次修正属于提示词护栏，不是 OpenClaw 核心代码修复；在极端情况下模型仍可能误用工具，但概率会明显下降。
- 回滚点（commit / 文件备份）: 直接回退 `workspace/SOUL.md` 与 `workspace/AGENTS.md` 本次新增段落。

### 下一步动作（第一条命令）

- `/oc-review OC-023`

### 给下一位 Agent 的说明

- 待确认问题: 若用户真正想要的是“创建自定义 skill 的能力”，更合适的下一步应是新建一个自定义 skill 模板，而不是继续寻找不存在的 `skill-creator` 插件。
- 前提假设: 当前结论基于本机 OpenClaw 2026.3.2 的 stock 插件源和 npm 官方命名空间。
- 必读上下文文件:
  - `docs/TASK_BOARD.md`
  - `docs/handoffs/2026-03-08.md`
  - `workspace/AGENTS.md`
  - `workspace/SOUL.md`